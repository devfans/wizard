builds:
  - binary: wizard
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
# Archive customization
archives:
  - format: tar.gz
    replacements:
      amd64: 64-bit
      darwin: macOS
    files:
      - LICENSE
      - README.md
